apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: items-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: discounts-deployment
  minReplicas: {{ .Values.items.hpa.minReplicas }}
  maxReplicas: {{ .Values.items.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.items.hpa.targetCPUUtilizationPercentage }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
    scaleUp:
      stabilizationWindowSeconds: 0
