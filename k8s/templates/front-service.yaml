apiVersion: v1
kind: Service
metadata:
  name: front-service
  labels:
    app: front
spec:
  selector:
    app: front
  ports:
    - name: http
      port: {{ .Values.front.port }}
      targetPort: {{ .Values.front.port }}
      protocol: TCP
  type: ClusterIP